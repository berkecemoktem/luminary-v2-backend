/**
 * Exams module. Owns exams, import drafts, published results and their
 * revisions. Exposes the result reading contract and the
 * {@code ExamResultsPublished} and {@code ExamResultsCorrected} events.
 */
package com.luminary.exams;