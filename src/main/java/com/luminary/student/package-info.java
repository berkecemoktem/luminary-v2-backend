/**
 * Student module. Owns the tenant-scoped student profile, grade level and basic
 * goal/target. Exposes the authorized profile and the minimal student context
 * consumed by other modules.
 */
package com.luminary.student;