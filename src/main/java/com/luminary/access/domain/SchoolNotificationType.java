package com.luminary.access.domain;

/**
 * Kind of school-side notification generated when an invitation is acted
 * upon by the invited student.
 */
public enum SchoolNotificationType {
    INVITE_ACCEPTED,
    INVITE_REJECTED
}