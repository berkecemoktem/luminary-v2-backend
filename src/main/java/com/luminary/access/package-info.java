/**
 * Access module. Owns identity mapping, membership and role control, session,
 * invitation security and the active working context. Exposes the authorized
 * principal/tenant and membership queries, plus the identity provider adapter.
 */
package com.luminary.access;